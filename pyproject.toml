[project]
name = "下注&結算排程-service"
version = "0.1.0"
description = "下注 / 結算 FastAPI service"
requires-python = ">=3.10"
dependencies = [
    "httpx>=0.27.0",
    "requests>=2.33.1",
    "fastapi>=0.110.0",
    "pydantic>=2.0.0",
    "python-crontab>=3.0.0",
    "uvicorn>=0.27.0",
]
